import 'package:flutter/material.dart';

class MarshallForms extends StatelessWidget {
  const MarshallForms({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Marshall Forms"),
      ),
    );
  }
}
